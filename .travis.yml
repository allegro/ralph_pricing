language: python
python:
  - "2.7"
# command to install dependencies, e.g. pip install -r requirements.txt --use-mirrors
install:
  - pip install git+https://github.com/allegro/ralph.git@develop --allow-all-external --allow-unverified ipaddr --allow-unverified postmarkup --allow-unverified python-graph-core
  - pip install git+https://github.com/allegro/ralph_assets.git@develop
  - pip install . --use-mirrors
# command to run tests, e.g. python setup.py test
script: DJANGO_SETTINGS_PROFILE=test-pricing ralph test ralph_pricing
